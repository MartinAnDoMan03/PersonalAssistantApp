<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- permission (jika butuh internet) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_checkmark"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_checkmark"
        android:supportsRtl="true"
        android:theme="@style/Theme.YourAssistantYora"
        tools:targetApi="31">

        <!-- SPLASH ACTIVITY - ENTRY POINT APP -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- MAIN ACTIVITY - HOST NAVIGATION (NavHost + AppNavigation) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.YourAssistantYora" />

        <!-- ✅ semua ACTIVITY lain yang dulu dipakai layar:
             - LoginActivity
             - RegisterActivity
             - HomeActivity
             - ProfileActivity
             - ForgotPasswordActivity
             - CheckEmailActivity
             - NoteActivity
             - NoteDetailActivity
             - CreateNoteActivity
             - DailyActivity
             - WeeklyActivity
             - MonthlyActivity
             - TeamActivity
             - CreateTeamActivity
             - JoinTeamActivity
             - TeamDetailActivity
           SUDAH TIDAK PERLU dideklarasikan lagi di sini,
           karena sekarang layarnya di-handle di AppNavigation (NavHost).
        -->

    </application>

</manifest>
